# DevNote v0.3.0 — Manual E2E Test Checklist

Open this file side-by-side during testing. Check off each item as you verify it.

---

## Setup & Upgrade

- [ ] Fresh install (no prior DevNote) → Setup state shown, credentials saved, Idle with empty Recent Notes
- [ ] Upgrade from v0.2.0 → credentials carry over, no Setup prompt, Idle loads directly
- [ ] Upgrade from v0.2.0 with existing draft → draft banner shows above Idle, Recent Notes below
- [ ] `devnote.db` file created in `globalStorageUri` on first activation

## Idle State — Recent Notes Display

- [ ] Empty state shows: "Your notes will appear here once you generate your first one."
- [ ] After first note sync → note appears as first row (title + branch + relative date)
- [ ] After second note sync → newest note is first, older note is second
- [ ] Relative date shows correctly: "just now", "2h", "1d", "3d", etc.
- [ ] Branch name displays correctly for feature branches
- [ ] Branch name shows "main" for notes generated on the base branch
- [ ] Long title truncates gracefully (no overflow, no horizontal scroll)
- [ ] Long branch name truncates gracefully
- [ ] List scrolls natively when 10+ notes exist (VS Code sidebar scroll)
- [ ] 20+ notes render without lag or visual issues
- [ ] Recent Notes section hidden during Form state
- [ ] Recent Notes section hidden during Generating state
- [ ] Recent Notes section hidden during Preview state (new note)
- [ ] Recent Notes section hidden during Syncing state
- [ ] Recent Notes section hidden during Success state
- [ ] Recent Notes section hidden during Error states
- [ ] Recent Notes section visible only in Idle state

## Generate + Sync Flow (unchanged + new memory step)

- [ ] Full happy path: Generate Doc → Form → Generate → Preview → Save Note → Syncing → Success
- [ ] After success → click "Back to Generate Doc" → Idle → new note is in Recent list
- [ ] Notion page was created successfully (verify in Notion)
- [ ] `devnote.db` contains the new row (verify via export or code inspection)
- [ ] Draft recovery still works if sync fails (draft banner appears on next open)
- [ ] Duplicate handling still works (Append / Replace / Cancel)

## Click Past Note — Historical Preview

- [ ] Click a row in Recent Notes → Preview state loads instantly
- [ ] Preview shows correct title
- [ ] Preview shows correct summary
- [ ] Preview shows correct "What Changed" bullets
- [ ] Preview shows correct "Why" section
- [ ] Preview shows correct "Key Decisions" section
- [ ] Preview shows correct "Files Affected" list
- [ ] "Back" button shown at top
- [ ] "Save Note" button NOT shown (hidden for historical)
- [ ] "Discard" button NOT shown (hidden for historical)
- [ ] "Open in Notion" link shown at bottom (subtle)
- [ ] Click "Open in Notion" → opens correct Notion page in browser
- [ ] Click "Back" → returns to Idle state with Recent list intact
- [ ] Works offline (WiFi off) — proves data comes from SQLite, not Notion

## Clear All Memory

- [ ] Gear icon → Settings → "Clear all memory" link visible at bottom (red text)
- [ ] Click "Clear all memory" → confirmation popup appears
- [ ] Popup shows warning icon
- [ ] Popup shows "Clear all memory?" headline
- [ ] Popup shows body text about permanent deletion + Notion safety
- [ ] Export checkbox shown, unchecked by default
- [ ] "Cancel" button has default focus (not "Clear all")
- [ ] Click Cancel → popup closes, nothing changed, list intact
- [ ] Press ESC → popup closes, nothing changed
- [ ] Click "Clear all" without export → notes deleted → toast shown → list empty
- [ ] Toast says "Memory cleared. Your Notion pages are untouched." (or similar)
- [ ] After clear → Idle shows empty state message again
- [ ] After clear → generate new note → list starts fresh with one row
- [ ] Notion pages still exist after clear (verify in Notion)
- [ ] Check export checkbox → click "Clear all" → save dialog opens
- [ ] Save dialog → pick location → JSON file saved → THEN notes deleted
- [ ] Saved JSON file is valid JSON (open and inspect)
- [ ] Saved JSON contains all notes that existed before clear
- [ ] Cancel save dialog → nothing deleted, notes still in list (critical safety test)

## Persistence

- [ ] Close sidebar → reopen → Recent Notes list persists (same rows)
- [ ] Reload VS Code window → Recent Notes list persists
- [ ] Fully quit and restart VS Code → Recent Notes list persists
- [ ] `devnote.db` file exists on disk at expected `globalStorageUri` path

## Error Scenarios

- [ ] Delete `devnote.db` manually while VS Code is open → next Idle refresh handles gracefully
- [ ] Corrupt `devnote.db` manually → on next activation, old file renamed to `.corrupt-{timestamp}`, fresh DB created, toast shown
- [ ] If WASM fails to load (simulate) → extension still works for generate/sync, Recent section shows "Memory unavailable" message

## Coexistence with Existing Features

- [ ] Draft banner + Recent Notes coexist in Idle (draft on top, list below)
- [ ] Generate Doc button still enabled/disabled based on branch detection
- [ ] Gear icon still opens Settings
- [ ] Settings back button still works
- [ ] All v0.2.0 error states still work (generate error, sync error)
- [ ] Keyboard shortcut Ctrl+Alt+D still works (opens sidebar + triggers generate)

---

**Total: ~65 checks. Estimated testing time: 30-45 minutes.**
